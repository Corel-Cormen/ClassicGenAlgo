From 9b01355fab5bb601b2a30c26c4ba4e81c6947f2d Mon Sep 17 00:00:00 2001
From: CorelCormen <kornel1997k@gmail.com>
Date: Tue, 4 Nov 2025 17:27:05 +0100
Subject: [PATCH] Benchmark functions show and save plot

---
 benchmark_functions.py | 7 ++++++-
 1 file changed, 6 insertions(+), 1 deletion(-)

diff --git a/benchmark_functions.py b/benchmark_functions.py
index 43a3ece..25fb900 100644
--- a/benchmark_functions.py
+++ b/benchmark_functions.py
@@ -283,6 +283,8 @@ class BenchmarkFunction(ABC):
         asHeatMap: bool = False,
         resolution: int = 50,
         showPoints: Iterable[float] = [],
+        savePath: str = "",
+        show: bool = True
     ):
         if self._n_dimensions > 2:
             raise ValueError(
@@ -381,7 +383,10 @@ class BenchmarkFunction(ABC):
             ax.set_xlabel("x")
             ax.set_ylabel("y")
 
-        plt.show()
+        if savePath != "":
+            plt.savefig(savePath, dpi=300, bbox_inches='tight')
+        if show:
+            plt.show()
 
     def testLocalMinimum(
         self,
-- 
2.50.1.windows.1

